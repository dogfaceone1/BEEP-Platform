<phone:PhoneApplicationPage
    x:Class="Car_Control.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->


        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <TextBlock x:Name="steeringText" HorizontalAlignment="Left" Margin="89,134,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top"/>
            <TextBlock x:Name="throttleText" HorizontalAlignment="Left" Margin="229,436,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top"/>
            <Slider x:Name="steering" HorizontalAlignment="Left" Margin="10,45,0,0" VerticalAlignment="Top" Width="436" Maximum="100" ValueChanged="steering_ValueChanged" Value="50" Foreground="{x:Null}" IsEnabled="False"/>
            <Slider x:Name="throttle" HorizontalAlignment="Left" Margin="363,156,0,0" VerticalAlignment="Top" Orientation="Vertical" Height="554" Maximum="100" Value="50" Foreground="{x:Null}" ValueChanged="throttle_ValueChanged_1" IsEnabled="False"/>
            <TextBlock x:Name="stateText" HorizontalAlignment="Left" Margin="23,243,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top"/>
            <Button x:Name="connectBT" Content="Connect" HorizontalAlignment="Left" Margin="23,166,0,0" VerticalAlignment="Top" Click="connectBT_Click"/>
            <CheckBox x:Name="lightsCB" Content="Head Lights" HorizontalAlignment="Left" Margin="23,275,0,0" VerticalAlignment="Top" Checked="lightsCB_Checked" Unchecked="lightsCB_Unchecked"/>
            <Slider x:Name="tilt" HorizontalAlignment="Left" Margin="46,479,0,0" VerticalAlignment="Top" Orientation="Vertical" Height="203" Maximum="100" Value="50" Foreground="{x:Null}" IsEnabled="True" ValueChanged="tilt_ValueChanged"/>
            <Button x:Name="sonarBT" Content="Sonar" HorizontalAlignment="Left" Margin="23,352,0,0" VerticalAlignment="Top" Width="214" Click="sonarBT_Click"/>

        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>